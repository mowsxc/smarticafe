# 🗺️ 项目分支和访问指南

**更新时间**: 2026-01-17 21:20

---

## 📍 当前分支状态

### 你现在在哪里？
```
✅ 当前分支: refactor/api-and-naming (重构分支)
📦 备份分支: master (原始版本)
🏷️  备份标签: v2.0.0-port-update-20260117-210324
```

### 分支对比

| 分支 | 说明 | 用途 | 最新提交 |
|------|------|------|----------|
| **master** | 原始版本 | 稳定版本，不会改动 | `3e27a49` |
| **refactor/api-and-naming** ⭐ | 重构版本 | 正在开发，添加了新API | `063225d` |

---

## 🌐 如何访问应用

### 方式一：Tauri桌面应用（推荐）

**当前运行的是**: 重构分支版本

```bash
# 已经在运行中
npm run tauri dev
```

**访问方式**: 
- 自动打开的桌面窗口
- 标题: "创新意 电竞馆 - Smarticafe Pro"

**特点**:
- ✅ 完整功能
- ✅ 真实数据
- ✅ 所有API都可用

---

### 方式二：浏览器版本

**当前运行的是**: 重构分支版本

```bash
# 已经在运行中
npm run dev
```

**访问地址**: 
```
http://localhost:32520
```

**特点**:
- ✅ 可以访问
- ⚠️  部分功能受限（Tauri API不可用）
- ✅ HTTP API可用

---

### 方式三：API测试页面（新增）

**测试所有API端点**

**访问方式**:
```powershell
# 在浏览器中打开
start api-test.html
```

**或者直接访问**:
```
file:///e:/smarticafe/smarticafe-v2.0.0/api-test.html
```

**功能**:
- 测试商品API
- 测试美团API
- 测试交班API
- 测试财务API

---

## 🔄 如何切换分支

### 切换到原始版本（master）

```bash
# 停止当前服务
# 按 Ctrl+C 停止 npm run dev 和 npm run tauri dev

# 切换分支
git checkout master

# 重新启动
npm run dev
npm run tauri dev
```

### 切换回重构版本

```bash
# 停止当前服务
# 按 Ctrl+C

# 切换分支
git checkout refactor/api-and-naming

# 重新启动
npm run dev
npm run tauri dev
```

---

## 📊 版本差异

### Master分支（原始版本）
```
端口: 
  - 前端: 32510
  - 后端: 3030

API端点:
  ✅ /api/products

变量命名:
  ❌ 使用拼音 (wangfei, shouhuo, zhichu等)
```

### Refactor分支（重构版本）⭐ 当前
```
端口:
  - 前端: 32520
  - 后端: 32521

API端点:
  ✅ /api/products
  ✅ /api/meituan/orders (新增)
  ✅ /api/shift/calculation (新增)
  ✅ /api/finance/accounting (新增)

变量命名:
  🔄 正在重构 (拼音 → 英文)
  
新增文件:
  ✅ src/api/types.ts (类型定义)
  ✅ src/api/meituan.ts (美团API)
  ✅ src/api/shift.ts (交班API)
  ✅ src/api/finance.ts (财务API)
  ✅ api-test.html (测试页面)
```

---

## 🎯 快速访问清单

### 当前运行的服务（重构版本）

| 服务 | 地址 | 状态 |
|------|------|------|
| 前端开发服务器 | http://localhost:32520 | ✅ 运行中 |
| 后端API服务器 | http://localhost:32521 | ✅ 运行中 |
| Tauri桌面应用 | 自动打开的窗口 | ✅ 运行中 |

### API端点

| API | 地址 | 状态 |
|-----|------|------|
| 商品列表 | http://127.0.0.1:32521/api/products | ✅ 有数据 |
| 美团订单 | http://127.0.0.1:32521/api/meituan/orders | ⚠️  空数据 |
| 交班计算 | http://127.0.0.1:32521/api/shift/calculation | ⚠️  默认值 |
| 财务记账 | http://127.0.0.1:32521/api/finance/accounting | ⚠️  默认值 |

---

## 💡 常见问题

### Q: 我现在看到的是哪个版本？
**A**: 重构版本 (refactor/api-and-naming)
- 前端端口是 32520
- 后端端口是 32521

### Q: 如何确认我在哪个分支？
**A**: 运行命令查看
```bash
git branch
# 带 * 号的就是当前分支
```

### Q: 重构版本和原版有什么区别？
**A**: 
1. **端口不同**: 32520/32521 vs 32510/3030
2. **API更多**: 新增了3个API端点
3. **代码更规范**: 添加了TypeScript类型定义

### Q: 我想回到原版怎么办？
**A**: 
```bash
git checkout master
```

### Q: 重构完成后怎么合并？
**A**: 
```bash
# 先切换到master
git checkout master

# 合并重构分支
git merge refactor/api-and-naming
```

---

## 🚀 下一步

### 继续重构（推荐）
```bash
# 确保在重构分支
git checkout refactor/api-and-naming

# 查看进度
code REFACTOR_PROGRESS.md
```

### 测试API
```bash
# 打开测试页面
start api-test.html
```

---

**提示**: 
- 🟢 绿色 = 正常运行
- 🟡 黄色 = 部分功能
- 🔴 红色 = 需要注意

**当前状态**: 🟢 重构分支运行正常，可以继续开发
