# SmartiCafe

一个面向门店场景的桌面端/网页端管理系统示例（Tauri + Vue）。

## 运行

- 复制 `.env.example` 为 `.env.local`，填写你自己的 Supabase 信息（如需启用云同步）。
- 安装依赖并启动：
  - `npm install`
  - `npm run dev`

> 本仓库不包含任何真实业务数据、账号与密钥。

## 开发协作

- AI/协作者请先阅读：`AGENTS.md` 与 `docs/INDEX.md`。
